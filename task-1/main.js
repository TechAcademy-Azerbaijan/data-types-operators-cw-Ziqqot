const prompt = require('prompt');
prompt.start();


prompt.get('input', function (err, result) {
  
    var number=parseInt(result.input); 
var s=6*9
  var v=3**3
var sum=s
var sam=v
console.log(sum,sam);


  
});
