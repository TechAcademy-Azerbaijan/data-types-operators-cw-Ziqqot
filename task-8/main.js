const prompt = require('prompt');
prompt.start();


prompt.get('input', function (err, result) {
  
     let input=(result.input);"123,10"
    let numbers=input.split(',');['123','10']
    let  n=parseInt([0]);"123"
    let x=parseInt([1]);"10"
    let c=3
    let sum=n/x;
    let sim=parseInt(sum)+c
    console.log(sim);
  
});
